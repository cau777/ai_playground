use crate::ops::model_source::ModelSource;

pub struct TaskManager {
    source: ModelSource
}

impl TaskManager {
    pub fn new(source: ModelSource) -> Self {
        Self {source}
    }

    pub fn get_task(){

    }
}