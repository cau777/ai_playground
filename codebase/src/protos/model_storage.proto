syntax = "proto3";

package proto_data;

message NdArrayData {
  repeated uint32 shape = 1;
  repeated float numbers = 2;
}

message PairData {
  string key = 1;
  repeated NdArrayData arrays = 2;
}

message ModelStorageData {
  repeated PairData pairs = 1;
}