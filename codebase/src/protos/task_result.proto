syntax = "proto3";

import "common.proto";
package proto_data;

message TestTaskData {
  uint32 version = 1;
  uint32 batch = 2;
  double accuracy = 3;
 }

message TaskResultData {
  oneof result {
    ModelStorageData delta = 2;
    TestTaskData test_data = 3;
  }
}